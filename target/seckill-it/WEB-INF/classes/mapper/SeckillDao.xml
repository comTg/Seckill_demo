<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.vetoer.dao.SeckillDao">
    <!-- 为DAO接口方法提供sql语句配置
    即针对dao接口中的方法编写我们的sql语句-->
    <update id="reduceNumber">
        update seckill
        set number = number+1
        where seckill_id=#{seckillId}
        and start_time <![CDATA[ <= ]]>#{killTime}
        and end_time >= #{killTime}
        and number>0;
    </update>
    <select id="queryById" resultType="Seckill" parameterType="long">
        select *
        from seckill
        where seckill_id=#{seckillId}
    </select>
    <select id="queryAll" resultType="Seckill">
        select *
        from seckill
        ORDER BY create_time DESC 
    </select>
    <select id="queryPage" resultType="Seckill">
        select *
        from seckill
        ORDER BY create_time DESC
        limit #{offset},#{limit}
    </select>
    <select id="getProductsCount" resultType="Integer">
        select count(seckill_id)
        from seckill
    </select>
    <select id="queryByName" resultType="Seckill">
        select seckill_id,name,number,start_time,end_time,create_time
        from seckill
        where name like #{name}
    </select>
    <!--mybatis调用存储过程-->
    <select id="killByProcedure" statementType="CALLABLE">
        call execute_seckill(
                #{seckillId,jdbcType=BIGINT,mode=IN },
                #{id,jdbcType=BIGINT,mode=IN },
                #{killTime,jdbcType=TIMESTAMP,mode=IN },
                #{result,jdbcType=INTEGER,mode=OUT }
        )
    </select>
</mapper>